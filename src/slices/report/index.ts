/**
 * Public boundary of the `report` slice.
 */

export {
  formatCounts,
  formatDeclined,
  formatInjection,
  formatOneLiner,
  formatReport,
  formatRuleCatalogue,
  formatStatus,
  formatWatching,
  languageEmoji,
  type StatusTheme,
} from "./format.js";
